# 第8章：建系统的通用模式

> **\"Die Grenzen meiner Sprache bedeuten die Grenzen meiner Welt.\"**
> **\"语言的极限就是我的世界的极限。\"**
>
> —— Ludwig Wittgenstein, Tractatus Logico-Philosophicus (5.6)

> 第七章给了你五步操作循环。其中第四步"建系统"最让人兴奋，但也最容易让人卡住——你知道了应该建系统，但不知道从哪里开始。
>
> 本章给你三种经过验证的通用系统模式。你不需要从零发明——只需要选择一种适配自己领域的模式，然后按它的结构填充内容。

## 开场：你已经在建系统，只是不知道

你觉得自己没有建过系统？

想一想：你有没有一个写着"每次发版前检查这些"的文档？你有没有一个"新项目立项的标准流程"？你有没有一个"判断这个方案靠不靠谱"的心算清单？

如果有——你已经在建系统了。只是你建的是"个人版本的"——只有你自己知道、只有你自己能用、别人接手就废。

建系统的三个通用模式，就是把这种个人经验转化成可以复制、可以检验、可以改进的结构化形式。从无序的重复劳动变成可复用的判断工具。

---

## 8.1 模式A：验证回路——确保产出质量

**适用场景**：你的主要问题是"产出质量不稳定"。

这是三种模式里最通用的。当你需要一个AI、一个人或一个流程的产出质量得到保障时使用。

**结构：**

```
输入 → 自动化检查 → 人工抽样 → 反馈闭环
```

**怎么理解这个结构：**

- **输入**：需要被验证的东西——一段代码、一篇文章、一个决策方案、一份设计稿
- **自动化检查**：能用规则、脚本、工具自动完成的部分。比如语法检查、格式规范、安全扫描
- **人工抽样**：自动化检查不能覆盖的——用人的判断来做。一次检查所有东西成本太高，所以抽检
- **反馈闭环**：发现的错误和改进建议，送回输入端——让下一次的输入质量越来越高

**经典实例：L1-L4分层验证体系**

这个模式最完整的实现，就是本书前面独立成篇的"四层验证体系"。简单来说：

| 层级 | 验证什么 | 谁来做 |
|---|---|---|
| L1 领域验证 | "AI输出的内容对不对" | 规则+测试 |
| L2 元领域验证 | "验证回路本身能不能覆盖失效模式" | 规则+人工抽样 |
| L3 自然哲学验证 | "逻辑自洽性和因果链" | 人工+工具辅助 |
| L4 哲学元验证 | "验证标准本身是否成立" | 人工（定期校准） |

每一层覆盖上一层的盲区。如果你需要保证AI辅助产出的质量——不是替代人工审查，而是让审查聚焦在机器做不了的那部分——这就是你要的模式。

但注意：**不要一开始就建四层。** 从L1开始。先建一个自动化的基础检查，让它跑起来。当它的漏检率开始出现时，再建L2来检查L1的盲区。逐层搭建，而不是一次性设计。建的太快比建得太慢的问题更严重——因为你会在系统还没验证的时候就开始信任它。

**其他领域的适配：**

*内容创作* —— AI写稿，你审稿：
- 自动化检查 → 字数、关键词密度、链接完整性、敏感词
- 人工抽样 → 每10篇抽1篇深度阅读，判断逻辑是否通顺、事实是否准确
- 反馈闭环 → 针对发现的问题更新"写作规范库"，让下次AI产出时自动避免

*管理决策* —— AI生成方案，你拍板：
- 自动化检查 → 方案的关键数据是否缺失、风险评估是否存在
- 人工抽样 → 对高风险方案做深度评审，低风险方案走快速通道
- 反馈闭环 → 记录每次判断的"预期 vs 结果"，积累成更精确的决策模型

*教育/训练* —— AI批改作业，教师复核：
- 自动化检查 → 标准答案匹配、格式规范、抄袭检测
- 人工抽样 → 每班抽5-8本深度批改，发现共性错误
- 反馈闭环 → 共性错误→更新教学重点→更新AI批改规则

**验证回路的核心原则：你要做的不是"防止所有错误"，而是"把错误控制在可接受的范围内"。** 试图做到零缺陷的系统，会把大部分资源浪费在检查那些本来就不会出问题的事情上。

### 验证回路的正交性原则

多层的验证回路有一个隐藏的陷阱：**两层可能在共享同一个盲区假设。**

举例：你的L1自动化检查假设"输入格式是标准的"，你的L2校准追踪器假设"过去的失效模式能预测未来的失效模式"。当AI用一种全新的输出格式（比如结构化的JSON变成了自由文本）出现时——L1和L2会同时失效。不是因为哪一层弱，是因为它们共享了"输出是可预测的"这个假设。

**检查你的验证回路是否正交：**

建完每一层之后，停下来问三个问题：

1. **这一层检查的问题类型，和下一层是否不同？** L1问"输出对不对"，L2问"检查规则靠不靠谱"。如果这两层问的是同一个问题——它们大概率共享盲区。

2. **同一错误进入多层时，每层的反应是否不同？** 一个格式错误应该被L1抓住，一个"所有检查都通过了但结论有问题"的深度错误应该被L3抓住。如果多层对同一错误有相同反应——它们在重复劳动，不是互补验证。

3. **每层的失败方式是否正交？** L1的失败是"规则不够细"，L2的失败应该是"框架假设错了"，L3的失败应该是"因果模型不适用"。如果两层的失败方式都是"规则不够细"——你只有一层，只不过复制了两次。

**如果你发现两层之间有共享盲区：不要试图通过'加固一层'来解决——加一个不同失败模式的新层，或者调整现有层的设计让它从不同的方向失败。**

---

## 8.2 模式B：判断框架——在不确定性下做决策

**适用场景**：你的主要问题是"决策经常后悔"。

验证回路的假设是"产出已经存在"——你是在验证已发生的东西。但很多场景下，产出还不存在，你需要做的是在多个未知的选项中选择一个。

**结构：**

```
提取决策变量 → 确定判定阈值 → 设计反证测试 → 嵌入反馈
```

**提取决策变量**：列出一个决策需要看的几件事。不要多——七个以内。超过七个变量，你的框架本身就不用了。

举一个实际的例子。假设你是一个产品经理，需要判断一个功能要不要做。你的决策变量可以这样拆：

- **用户需求强度**：有多少用户要？他们有多需要？不是一个"A/B/C"的打分——可以量化，比如">20%的用户有明确诉求=3分，5-20%=2分，<5%=1分"
- **技术实现成本**：需要多少人/多少时间？同样量化——"<1人月=3分，1-3人月=2分，>3人月=1分"
- **商业价值**：预期对营收/留存/转化率的贡献
- **竞争压力**：不做的话，竞争对手会在多久内超越？"已有人在做=3分，半年内会有人做=2分，没人在做=1分"
- **团队能力匹配度**：现有团队做这个方案的难度

就这五个。不要更多。

**确定判定阈值**：每个变量到什么程度算"可以"、到什么程度算"不行"。

把"我觉得可以"变成：
- 用户需求强度≥7/10
- 技术成本≤3人月
- 商业价值≥估算的100万/年
- 竞争压力≤半年窗口
- 团队能力匹配度≥70%

阈值不是为了限制你——是为了让你有依据说"不"。没有阈值的时候，你拒绝一个提案只有一个理由："我觉得不行"——这在团队里是没有分量的。有了阈值，你可以说："这个功能的用户需求强度是4/10，我们的标准是7/10。"前者是感觉，后者是标准。

**设计反证测试**：这是最容易被跳过的，也是最关键的一步。找一个变量，假设它反方向成立——如果你判断"可以做"，问自己"什么条件下它就不该做？"如果你判断"不能做"，问自己"什么条件下它就值得重审？"反证测试的作用不是推翻结果——是防止你把一个不成熟的判断当作确定信号。

**嵌入反馈**：每个决策三个月后，回头检视。你的预期和实际结果差距在哪？是某个变量的权重设错了？是你忽略了一个重要的变量？把这个学习带回到下一次决策中。

**经典实例：战略选择的优先级矩阵**

你不是在做学术上的"完美决策"——你是在有限的认知和有限的时间里做"足够好的决策"。判断框架的价值不是帮你找到最优解——是防止你犯最明显的错误。

具体来说，它会帮你拦住三种最常见的错误：

1. **回音室决策**——你身边所有人都说好，你就是看不到风险。反证测试会强制你去看那些反方向的证据。
2. **激情决策**——你觉得这个方案太兴奋了，以至于你忽略了一个明显的致命问题。判断阈值会在你兴奋的时候拉你回来看数字。
3. **分析瘫痪**——变量太多，决定不了。框架把你的决策简化为五个问题，回答了就可以拍板。

---

## 8.3 模式C：培训系统——让新手快速达到判断标准

**适用场景**：你的主要问题是"知识无法传递"。

验证回路保证质量，判断框架保证决策——但还有一个更深层的问题：这些判断力怎么传递给别人？你不可能永远自己做每一个判断。你需要让团队里的其他人也能达到你的判断标准。

**结构：**

```
提取"好"和"不好"的案例对 → 设计识别练习 → 搭建反馈机制 → 升级案例库
```

**提取案例对**：找三个你做过的"好"判断和三个你做过的"不好"判断。每一对要写清楚：你当时看了什么信息、你判断的时候依赖什么信号、结果怎样。尽量具体。不要写"我选了正确的方案"——写"我当时看的是用户留存率数据，它虽然不高但趋势在上升，所以我判断应该继续投入"。

**设计识别练习**：把这些案例对变成练习题——给对方看同样的信息，让他做判断。然后对照你的判断。他的判断和你的判断之间的差距，就是需要训练的方向。

**搭建反馈机制**：不是一次性的训练——他做判断之后，你需要告诉他为什么你的判断和他的不同。这个"为什么"才是训练的实质。

**升级案例库**：训练框架本身也需要进化。每三个月加入新的案例对——最近发生的好的和不好的判断。随着时间推移，案例库反映的是你们团队一直在解决的问题类型。

**经典实例：《学透》精通五层学习路径**

如果你读过《学透》，你会认出这个模式在"精通五层"中的落地——跑通→拆解→参数→边界→封装。

这不只是学习路线——它也是一个培训系统。每一层都对应着一组判断力：你能不能跑通（基本准入门槛）、你能不能拆解（理解结构）、你能不能调参（理解因果关系）、你能不能识别边界（理解适用范围）、你能不能封装（理解可转移性）。

**培训系统 vs 验证回路 vs 判断框架：**

| | 验证回路 | 判断框架 | 培训系统 |
|---|---|---|---|
| 核心问题 | 质量怎么保证 | 决策怎么不后悔 | 能力怎么传递 |
| 用户 | 产出的消费者 | 决策者 | 新手 |
| 输出 | 一套验证流程 | 一套决策清单 | 一套训练材料 |
| 使用频率 | 每次产出 | 每次决策 | 每次训练 |

---

## 8.4 如何选择你需要的模式

一个简单的决策树：

```
你的主要问题是"产出质量不稳定"？
  ├─ 是 → 验证回路
  └─ 否 → 继续问

你的主要问题是"决策经常后悔"？
  ├─ 是 → 判断框架
  └─ 否 → 继续问

你的主要问题是"知识无法传递"？
  ├─ 是 → 培训系统
  └─ 否 → 检查你是不是根本不需要建系统——可能问题不是系统不够，
         而是你没有用现有的流程
```

**为什么要单列这个决策树？**

因为大多数人犯的错误不是"选错了模式"——而是**在不该建系统的时候建了系统**。

什么时候不该建系统？当你现有的流程只是"用得不太顺手"的时候。一个不太顺手的流程，可以通过优化来解决。但一个没有人用的系统，再完美也是浪费。

所以建系统之前确认三件事：

1. 你已经有了一个至少能跑的流程。系统化是在流程上盖房子，不是在空地上。
2. 你至少能用同一个流程经历过三次以上的重复。没有重复，就没有模式可以提取。
3. 你确认这个问题不是"换个工具就能解决"的。很多时候你需要的不是系统，是换一个更好用的工具。

---

## 本章收束

三种模式：验证回路（质量）、判断框架（决策）、培训系统（传递）。

现在你知道了每种模式的结构、适用的场景、和经典实例。下一章会讲一个更难的课题——不是怎么建系统，而是什么时候放弃它。

> **建系统不是写代码。**
> **建系统是在你和你的判断力之间，搭一座别人也能走的桥。**
